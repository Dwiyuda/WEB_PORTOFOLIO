---
import "../styles/global.css";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="canonical" href={canonical} />

    <meta property="og:type" content="profile" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:locale" content="en" />
    <meta property="og:image" content={new URL("/og.png", Astro.site)} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="Dwi Yuda. I audit my own work before someone else has to. GPA 3.86, 1,645 images, around 300 undergraduates, thesis graded A." />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="author" content="Dwi Yuda" />

    <link
      rel="preload"
      href="/fonts/literata.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
    <style is:global>
      /* Self-host, jadi tidak ada permintaan pihak ketiga sebelum teks pertama
         tampil. Variable font, satu berkas untuk seluruh rentang berat.
         Diperbarui lewat: node scripts/fetch-fonts.mjs */
      @font-face {
        font-family: "Bricolage Grotesque";
        src: url("/fonts/bricolage-grotesque.woff2") format("woff2");
        font-weight: 400 800;
        font-style: normal;
        font-display: swap;
      }
      @font-face {
        font-family: "Literata";
        src: url("/fonts/literata.woff2") format("woff2");
        font-weight: 400 700;
        font-style: normal;
        font-display: swap;
      }
      @font-face {
        font-family: "JetBrains Mono";
        src: url("/fonts/jetbrains-mono.woff2") format("woff2");
        font-weight: 400 600;
        font-style: normal;
        font-display: swap;
      }
    </style>

    {/* Supaya mesin pencari tahu halaman ini tentang satu orang, dan
        menampilkan nama serta tautan yang benar saat seseorang mencari
        "Dwi Yuda". Tanpa ini mereka menebak dari teks. */}
    <script
      type="application/ld+json"
      set:html={JSON.stringify({
        "@context": "https://schema.org",
        "@type": "Person",
        name: "Dwi Yuda",
        url: canonical.href,
        image: new URL("/og.png", Astro.site).href,
        jobTitle: "AI Engineer",
        email: "mailto:dwiiyudaaa@gmail.com",
        address: {
          "@type": "PostalAddress",
          addressLocality: "Pekanbaru",
          addressRegion: "Riau",
          addressCountry: "ID",
        },
        alumniOf: {
          "@type": "CollegeOrUniversity",
          name: "Universitas Islam Riau",
        },
        knowsAbout: [
          "Computer vision",
          "Deep learning",
          "Model evaluation",
          "Large language model applications",
        ],
        knowsLanguage: ["Indonesian", "English", "Arabic"],
        sameAs: [
          "https://github.com/Dwiyuda",
          "https://www.linkedin.com/in/dwiyuda",
        ],
      })}
    />

    {/* Dibaca sebelum paint supaya tema pilihan tidak berkedip saat muat.
        Terang tetap default: tanpa pilihan tersimpan, tidak ada yang di-set. */}
    <script is:inline>
      if (localStorage.getItem("theme") === "dark") {
        document.documentElement.dataset.theme = "dark";
      }
    </script>
  </head>

  <body>
    <a class="skip-link" href="#main">Skip to content</a>
    <div class="progress" aria-hidden="true"></div>

    <header class="masthead">
      <div class="shell shell--wide masthead__inner">
        <a class="masthead__name" href="#top">Dwi Yuda</a>

        <nav class="masthead__nav" aria-label="Sections">
          <a href="#timeline">Timeline</a>
          <a href="#experience">Experience</a>
          <a href="#projects">Projects</a>
          <a href="#contact">Contact</a>
        </nav>

        <button
          type="button"
          id="theme-toggle"
          class="masthead__toggle"
          aria-pressed="false"
          aria-label="Switch to dark theme"
        >
          <svg width="16" height="16" viewBox="0 0 16 16" aria-hidden="true">
            <path
              d="M13 9.5A5.5 5.5 0 0 1 6.5 3a5.5 5.5 0 1 0 6.5 6.5Z"
              fill="none"
              stroke="currentColor"
              stroke-width="1.3"
              stroke-linejoin="round"></path>
          </svg>
        </button>
      </div>
    </header>

    <main id="main">
      <slot />
    </main>

    {/* Satu dialog dipakai bersama seluruh gambar yang bisa diperbesar.
        <dialog> asli, jadi Esc, fokus, dan latar gelapnya sudah ditangani
        peramban. */}
    <dialog class="lightbox" id="lightbox">
      <button class="lightbox__close" type="button" aria-label="Close">
        <svg width="16" height="16" viewBox="0 0 16 16" aria-hidden="true">
          <path
            d="M4 4l8 8M12 4l-8 8"
            fill="none"
            stroke="currentColor"
            stroke-width="1.6"
            stroke-linecap="round"></path>
        </svg>
      </button>
      <img alt="" />
      <p class="lightbox__cap caption"></p>
    </dialog>

    <script>
      const toggle = document.getElementById("theme-toggle");
      const root = document.documentElement;

      const sync = () => {
        const dark = root.dataset.theme === "dark";
        toggle?.setAttribute("aria-pressed", String(dark));
        toggle?.setAttribute(
          "aria-label",
          dark ? "Switch to light theme" : "Switch to dark theme",
        );
      };

      toggle?.addEventListener("click", () => {
        const flip = () => {
          const dark = root.dataset.theme === "dark";
          if (dark) {
            delete root.dataset.theme;
            localStorage.removeItem("theme");
          } else {
            root.dataset.theme = "dark";
            localStorage.setItem("theme", "dark");
          }
          sync();
        };

        const reduced = matchMedia("(prefers-reduced-motion: reduce)").matches;
        if (document.startViewTransition && !reduced) {
          // Transisi ditolak kalau ada yang sedang berjalan atau dokumen
          // tak terlihat. Perubahan temanya tetap berlaku, jadi penolakan
          // itu diserap di sini alih-alih jatuh ke konsol.
          const vt = document.startViewTransition(flip);
          vt.ready.catch(() => {});
          vt.finished.catch(() => {});
        } else {
          flip();
        }
      });

      sync();

      // Perbesar gambar. Satu penangan di dokumen, bukan satu per gambar,
      // supaya gambar yang ditambahkan nanti ikut tanpa kode tambahan.
      const box = document.getElementById("lightbox") as HTMLDialogElement | null;

      if (box) {
        const boxImg = box.querySelector("img");
        const boxCap = box.querySelector(".lightbox__cap");

        document.addEventListener("click", (event) => {
          const target = event.target as HTMLElement;
          const img = target.closest<HTMLImageElement>(
            ".screen img, .shot img",
          );

          if (img && boxImg) {
            boxImg.src = img.currentSrc || img.src;
            boxImg.alt = img.alt;
            if (boxCap) {
              const cap = img
                .closest("figure")
                ?.querySelector("figcaption")?.textContent;
              boxCap.textContent = cap?.trim() ?? "";
            }
            box.showModal();
            return;
          }

          // Klik pada latar gelap menargetkan dialognya sendiri.
          if (target === box || target.closest(".lightbox__close")) {
            box.close();
          }
        });
      }

      // Menandai bagian yang sedang dibaca. Pitanya sempit di tengah layar,
      // jadi hanya satu tautan menyala pada satu waktu.
      const links = [
        ...document.querySelectorAll<HTMLAnchorElement>(".masthead__nav a"),
      ];
      const byId = new Map(
        links.map((a) => [a.getAttribute("href")?.slice(1) ?? "", a]),
      );

      if (byId.size) {
        const spy = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              byId
                .get(entry.target.id)
                ?.classList.toggle("is-current", entry.isIntersecting);
            });
          },
          { rootMargin: "-45% 0px -50% 0px" },
        );

        byId.forEach((_, id) => {
          const target = document.getElementById(id);
          if (target) spy.observe(target);
        });
      }
    </script>

    <style>
      .masthead {
        position: sticky;
        top: 0;
        z-index: 10;
        border-bottom: 1px solid var(--rule);
        background: color-mix(in srgb, var(--paper) 88%, transparent);
        backdrop-filter: blur(10px);
      }

      .masthead__inner {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
        padding-block: 0.85rem;
      }

      .masthead__name {
        font-family: var(--font-mono);
        font-size: var(--size-caption);
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--ink-soft);
        text-decoration: none;
        transition: color var(--t-fast) var(--ease);
      }

      .masthead__name:hover {
        color: var(--ink);
      }

      .masthead__nav {
        display: none;
        gap: 1.5rem;
      }

      @media (min-width: 46rem) {
        .masthead__nav {
          display: flex;
        }
      }

      .masthead__nav a {
        position: relative;
        font-size: var(--size-figure);
        color: var(--ink-soft);
        text-decoration: none;
        transition: color var(--t-fast) var(--ease);
      }

      .masthead__nav a:hover {
        color: var(--ink);
      }

      /* Garis bawah tumbuh dari kiri saat disentuh kursor. */
      .masthead__nav a::after {
        content: "";
        position: absolute;
        left: 0;
        right: 0;
        bottom: -0.35rem;
        height: 1px;
        background: currentColor;
        transform: scaleX(0);
        transform-origin: 0 50%;
        transition: transform var(--t-mid) var(--ease-out);
      }

      .masthead__nav a:hover::after,
      .masthead__nav a.is-current::after {
        transform: scaleX(1);
      }

      .masthead__nav a.is-current {
        color: var(--ink);
      }

      .masthead__nav a.is-current::after {
        background: var(--verified);
        height: 2px;
      }

      .masthead__toggle {
        display: grid;
        place-items: center;
        width: 2rem;
        height: 2rem;
        padding: 0;
        color: var(--ink-soft);
        background: transparent;
        border: 1px solid var(--rule);
        border-radius: 50%;
        cursor: pointer;
        transition:
          color 120ms ease,
          border-color 120ms ease;
      }

      .masthead__toggle:hover {
        color: var(--ink);
        border-color: var(--ink-soft);
      }

      .lightbox {
        max-width: min(92vw, 68rem);
        max-height: 92vh;
        padding: 0;
        color: var(--ink);
        background: var(--paper);
        border: 1px solid var(--rule);
        border-radius: 3px;
        overflow: visible;
      }

      .lightbox::backdrop {
        background: rgb(10 12 13 / 0.72);
        backdrop-filter: blur(3px);
      }

      .lightbox img {
        display: block;
        max-width: 100%;
        max-height: 80vh;
        width: auto;
        height: auto;
        object-fit: contain;
      }

      .lightbox__cap {
        margin: 0;
        padding: 0.75rem 1rem;
        border-top: 1px solid var(--rule);
      }

      .lightbox__cap:empty {
        display: none;
      }

      .lightbox__close {
        position: absolute;
        top: -0.75rem;
        right: -0.75rem;
        display: grid;
        place-items: center;
        width: 2rem;
        height: 2rem;
        padding: 0;
        color: var(--ink);
        background: var(--paper);
        border: 1px solid var(--rule);
        border-radius: 50%;
        cursor: pointer;
      }

      .lightbox__close:hover {
        border-color: var(--ink-soft);
      }
    </style>
  </body>
</html>
