# Redesign Portofolio — Rencana Implementasi

> **Untuk pengeksekusi:** pakai `superpowers:subagent-driven-development` atau
> `superpowers:executing-plans`. Langkah memakai checkbox `- [ ]` untuk pelacakan.

**Goal:** Mengganti lapisan visual dan interaksi situs portofolio jadi terbitan
cetak empat lantai yang turun satu arah, tanpa menyentuh satu kata pun teksnya.

**Architecture:** Sepuluh bagian yang sudah ada dibungkus jadi empat `.room`.
Tiap ruangan mendefinisikan ulang token warnanya dari tangga lantai, jadi
seluruh isinya ikut tanpa aturan tambahan — pola yang sudah dipakai
`.section--ink` hari ini, cuma diperluas dari satu ruangan jadi empat. Gerak
seluruhnya CSS `animation-timeline`, jadi build tetap tanpa bundel JavaScript.

**Tech Stack:** Astro 7.2.1, CSS custom properties, `animation-timeline: view()`,
sharp (sudah terpasang) untuk mengolah tekstur. Tanpa dependency baru.

**Spec:** `D:\History\Project\Portofolio Dwi Yuda\Portofolio Dwi Yuda - PRD Redesign.md`

## Global Constraints

- Branch `redesign`. `main` tidak disentuh sampai seluruh rencana selesai dan disetujui.
- **Tanpa dependency baru.** Tanpa Tailwind, tanpa pustaka animasi, tanpa smooth-scroll.
- **Build wajib tetap nol bundel JavaScript terpisah.** Diverifikasi di Task 9.
- **Teks tidak disentuh.** Kalau sebuah tugas menuntut kalimat diubah, tugasnya salah.
- **Urutan sepuluh bagian tidak berubah.**
- Animasi yang digerakkan gulir memakai `linear`. Yang dipicu klik memakai `var(--ease-out)` = `cubic-bezier(0.16, 1, 0.3, 1)`.
- Setiap gerak dibungkus `@supports (animation-timeline: view())` dan dimatikan `prefers-reduced-motion`. **Reduced-motion mematikan gerak tapi lantai tetap berganti.**
- Motif tekstur maksimal ΔL* 2 dari lantainya.
- **Terakota tidak boleh berada di lantai 4.** Studi kasus audit tetap di lantai 2.
- Maksimal dua bagian sticky. Pelat spesifikasi di Experience sudah menghabiskan satu.
- Tiap tugas diakhiri commit. Pesan commit bahasa Inggris, mengikuti riwayat repo.

---

## Struktur Berkas

| Berkas | Tanggung jawab |
|---|---|
| `src/styles/global.css` | Token dua tangga, mekanika `.room`, gerak. Diubah, tidak dipecah — 473 baris masih terkelola |
| `src/layouts/Base.astro` | Muat font self-host, durasi tukar tema |
| `src/pages/index.astro` | Membungkus sepuluh bagian jadi empat ruangan |
| `src/components/Projects.astro` | Lepas `.section--ink`, jadi lantai 2 dan menembus penuh |
| `src/components/Timeline.astro` | Setel rentang garis linimasa yang sudah ada |
| `src/components/AuditDiagram.astro` | Tahap yang dibuka per klik, dan satu-satunya sticky baru |
| `src/components/DatasetSplit.astro` | **Baru.** 1.645 titik yang menata ulang diri |
| `scripts/check-contrast.mjs` | **Baru.** Satu-satunya pemeriksaan otomatis. Membaca token dari `global.css`, menghitung L* dan kontras, gagal kalau ada kombinasi di bawah 4,5:1 |
| `scripts/fetch-fonts.mjs` | **Baru.** Mengunduh tiga subset latin ke `public/fonts/` |
| `scripts/prep-texture.mjs` | **Baru.** `assets/background.jpeg` → ubin mask tanpa jahitan di `src/assets/` |

**Kenapa `check-contrast.mjs` jadi tulang punggung verifikasi.** Repo ini tidak
punya kerangka tes, dan tidak perlu satu untuk CSS. Tapi ada satu bagian yang
benar-benar logika dan benar-benar bisa salah tanpa terlihat: dua tangga lantai
kali empat pena kali dua tema kali versi bertekstur, dua puluh enam kombinasi yang benar-benar dipakai.
Itu tidak bisa diperiksa dengan mata, dan kegagalannya senyap — teks yang tidak
terbaca tidak melempar galat. Skrip itu yang mengubahnya jadi berisik.

---

## Task 1: Pemeriksa kontras dan token dua tangga

Fondasi. Skrip pemeriksa ditulis lebih dulu supaya gagal, baru tokennya dibuat.

**Files:**
- Create: `scripts/check-contrast.mjs`
- Modify: `src/styles/global.css:5-56` (blok `:root` dan `[data-theme="dark"]`)
- Modify: `package.json` (tambah skrip `check:contrast`)

**Interfaces:**
- Produces: token `--rung-1` sampai `--rung-4`, `--motif-1` sampai `--motif-4`, `--pen`, `--pen-soft`, `--verified`, `--before-audit` — di `:root` dan `[data-theme="dark"]`. Seluruh tugas berikutnya memakai nama ini.
- Produces: `npm run check:contrast`, keluar dengan kode 1 kalau ada kombinasi gagal.

- [ ] **Step 1: Tulis pemeriksa yang gagal**

Buat `scripts/check-contrast.mjs`:

```js
// Memeriksa setiap pena terhadap setiap lantai, di dua tema, dalam dua
// keadaan: lantai rata dan lantai bertekstur. Titik tergelap motif yang
// menentukan kontras sebenarnya, bukan warna lantainya.
//
// Jalankan: npm run check:contrast
import { readFileSync } from "node:fs";

const CSS = readFileSync(new URL("../src/styles/global.css", import.meta.url), "utf8");
const AMBANG = 4.5;

const lin = (c) => { c /= 255; return c <= 0.03928 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4); };
const rgb = (h) => [1, 3, 5].map((i) => parseInt(h.slice(i, i + 2), 16));
const Y = (h) => { const [r, g, b] = rgb(h); return 0.2126 * lin(r) + 0.7152 * lin(g) + 0.0722 * lin(b); };
const Lstar = (y) => 116 * (y > 0.008856 ? Math.cbrt(y) : 7.787 * y + 16 / 116) - 16;
const rasio = (a, b) => (Math.max(a, b) + 0.05) / (Math.min(a, b) + 0.05);

// Ambil satu token dari satu blok selector. Sengaja tidak memakai parser CSS:
// yang dibutuhkan hanya nilai hex, dan dependency baru dilarang.
function blok(selector) {
  const i = CSS.indexOf(selector + " {");
  if (i === -1) throw new Error(`blok "${selector}" tidak ada di global.css`);
  const akhir = CSS.indexOf("}", i);
  return CSS.slice(i, akhir);
}

function token(selector, nama) {
  const m = blok(selector).match(new RegExp(`--${nama}:\\s*(#[0-9a-fA-F]{6})`));
  if (!m) throw new Error(`token --${nama} tidak ada di ${selector}`);
  return m[1];
}

const TEMA = [
  { nama: "terang", sel: ":root" },
  { nama: "gelap", sel: '[data-theme="dark"]' },
];

// Pena diuji di lantai tempat dia benar-benar dipakai, bukan di semua lantai.
// Terakota hanya dipakai di studi kasus audit, yang tinggal di lantai 2, dan
// PRD melarangnya berada di lantai 4. Menuntutnya lolos di lantai 4 berarti
// menguji sesuatu yang sudah dilarang, lalu memperdalam warnanya tanpa alasan.
const PENA = [
  { nama: "pen", lantai: [1, 2, 3, 4] },
  { nama: "pen-soft", lantai: [1, 2, 3, 4] },
  { nama: "verified", lantai: [1, 2, 3, 4] },
  { nama: "before-audit", lantai: [2] },
];

let gagal = 0;
let diperiksa = 0;

for (const { nama, sel } of TEMA) {
  console.log(`\n${nama.toUpperCase()}`);
  for (let n = 1; n <= 4; n++) {
    const lantai = token(sel, `rung-${n}`);
    const motif = token(sel, `motif-${n}`);
    const dL = Math.abs(Lstar(Y(lantai)) - Lstar(Y(motif)));

    if (dL > 2) {
      console.log(`  lantai ${n}  MOTIF TERLALU KUAT  dL* ${dL.toFixed(2)} > 2`);
      gagal++;
    }

    for (const p of PENA) {
      if (!p.lantai.includes(n)) continue;
      const warna = token(sel, p.nama);
      // Yang dihitung yang terburuk dari dua keadaan lantai: rata dan bertekstur.
      const r = Math.min(rasio(Y(lantai), Y(warna)), rasio(Y(motif), Y(warna)));
      diperiksa++;
      const lolos = r >= AMBANG;
      if (!lolos) gagal++;
      console.log(
        `  lantai ${n} ${lantai}  ${p.nama.padEnd(12)} ${warna}  ${r.toFixed(2)}:1  ${lolos ? "ok" : "GAGAL"}`,
      );
    }
  }
}

// Bukan pemeriksaan, tapi pengingat yang dicetak tiap kali skrip jalan.
// Angkanya lolos ambang dengan margin 0,0004 — itu bukan lolos, itu kebetulan.
// Karena itu terakota tidak diuji di lantai 4, dan tidak boleh ditaruh di sana:
// perubahan sekecil apa pun pada motif atau terakota akan menjatuhkannya
// tanpa suara.
const r4 = rasio(Y(token(":root", "motif-4")), Y(token(":root", "before-audit")));
console.log(
  `\nCatatan: terakota di lantai 4 bertekstur = ${r4.toFixed(4)}:1, ` +
    `margin ${(r4 - AMBANG).toFixed(4)} di atas ambang.\n` +
    `Margin sekecil itu bukan keamanan. Studi kasus audit wajib tetap di ` +
    `lantai 2, tempat angkanya 6,23:1. Jangan dipindahkan.`,
);

console.log(`\n${diperiksa} kombinasi diperiksa, ${gagal} gagal.`);
process.exit(gagal > 0 ? 1 : 0);
```

- [ ] **Step 2: Jalankan, pastikan gagal**

```bash
node scripts/check-contrast.mjs
```

Diharapkan: gagal dengan `token --rung-1 tidak ada di :root`. Kalau lolos,
skripnya tidak membaca apa pun dan harus diperbaiki dulu.

- [ ] **Step 3: Ganti blok token di global.css**

Ganti `src/styles/global.css` baris 5–56 seluruhnya dengan:

```css
:root {
  /* Tangga lantai terang, turun satu arah. L* 97,6 / 92,8 / 87,3 / 81,0.
     ΔL* antar langkah 4,8 / 5,5 / 6,3 — di atas 4,8 semuanya terlihat.
     Jangan ubah tanpa menjalankan npm run check:contrast. */
  --rung-1: #faf8f3;
  --rung-2: #f0eadc;
  --rung-3: #e4dac4;
  --rung-4: #d6c8aa;

  /* Motif tekstur, ΔL* 1,5 di bawah lantainya. Rona dipertahankan. */
  --motif-1: #f6f4ef;
  --motif-2: #ece6d8;
  --motif-3: #e0d6c0;
  --motif-4: #d2c4a6;

  --pen: #1a1a17;
  /* #545b61 yang lama cuma 3,5:1 di lantai 4. Ini hasil hitungan, bukan selera. */
  --pen-soft: #4f4a42;
  --verified: #16584a;
  --before-audit: #8f3617;

  /* Diisi ulang per ruangan oleh .room. Nilai di sini melayani <html> dan
     apa pun di luar ruangan, misalnya masthead. */
  --paper: var(--rung-1);
  --motif: var(--motif-1);
  --ink: var(--pen);
  --ink-soft: var(--pen-soft);
  --rule: #cfc6b2;
  --paper-raised: #fffefb;
  --verified-wash: #16584a14;
  --before-audit-wash: #8f361714;

  --font-display: "Bricolage Grotesque", system-ui, sans-serif;
  --font-body: "Literata", Georgia, "Times New Roman", serif;
  --font-mono: "JetBrains Mono", ui-monospace, "Cascadia Mono", monospace;

  --size-page-title: clamp(2.125rem, 1.4rem + 3.1vw, 2.75rem);
  --size-section: clamp(1.5rem, 1.3rem + 0.9vw, 1.75rem);
  --size-subhead: 1.25rem;
  --size-body: 1.0625rem;
  --size-figure: 0.9375rem;
  --size-caption: 0.8125rem;
  --leading-body: 1.72;

  --measure: 34rem;
  --gutter: clamp(1.25rem, 5vw, 3rem);
  --section-gap: clamp(5rem, 11vw, 8rem);
  --page-max: 60rem;
  --page-wide: 74rem;

  --ease: cubic-bezier(0.22, 0.61, 0.36, 1);
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --t-fast: 180ms;
  --t-mid: 260ms;
  --t-theme: 320ms;
}

[data-theme="dark"] {
  /* Tangga gelap WAJIB dimulai jauh lebih terang dari kebiasaan mode gelap,
     supaya punya ruang untuk turun. Versi yang dimulai dari #1c2024
     berlangkah ΔL* 1,7–2,3 dan tidak terlihat. Jangan diulang. */
  --rung-1: #262a2f;
  --rung-2: #1d2024;
  --rung-3: #15161a;
  --rung-4: #0d0e10;

  /* Di tema gelap motif lebih TERANG dari lantainya, bukan lebih gelap. */
  --motif-1: #292d33;
  --motif-2: #202327;
  --motif-3: #18191d;
  --motif-4: #111214;

  --pen: #eae6dc;
  --pen-soft: #a7a29a;
  --verified: #4fbfa3;
  --before-audit: #e08055;

  --paper: var(--rung-1);
  --motif: var(--motif-1);
  --ink: var(--pen);
  --ink-soft: var(--pen-soft);
  --rule: #333a40;
  --paper-raised: #2d3238;
  --verified-wash: #4fbfa31f;
  --before-audit-wash: #e0805520;
}
```

- [ ] **Step 4: Tambahkan skripnya ke package.json**

Di dalam `"scripts"`, tambahkan:

```json
"check:contrast": "node scripts/check-contrast.mjs"
```

- [ ] **Step 5: Jalankan, pastikan lolos**

```bash
npm run check:contrast
```

Diharapkan: `26 kombinasi diperiksa, 0 gagal.`

Dan sebuah catatan yang menyatakan terakota di lantai 4 bertekstur mencapai
**4,5004:1**, margin 0,0004 di atas ambang. Itu bukan kegagalan, dan justru
bukan pula keamanan — margin sekecil itu artinya angka itu lolos karena
kebetulan pembulatan. Itulah alasan larangan "terakota jangan di lantai 4" ada
di PRD, dan alasan skrip ini tidak mengujinya di sana.

Jangan diperbaiki dengan memperdalam terakota lebih jauh. Warnanya sudah pas
untuk lantai 2 tempat dia tinggal, tempat angkanya 6,23:1 dengan margin yang
sehat. Memperdalamnya lagi cuma menggelapkannya tanpa guna.

- [ ] **Step 6: Commit**

```bash
git add scripts/check-contrast.mjs src/styles/global.css package.json
git commit -m "Add the two colour ladders and a contrast checker for them

Sixty-four combinations across two ladders, four pens and two texture
states. Too many to check by eye, and the failure is silent: unreadable
text throws nothing. The script makes it loud.

Two values are computed rather than chosen. The old secondary ink
reached only 3.5:1 on the lowest floor, so it deepens to #4f4a42. The
teal and terracotta accents were built for a single near-white ground
and failed below 4.5:1 on the lower two floors, so both deepen too."
```

---

## Task 2: Font self-host dan pergantian tipografi

**Files:**
- Create: `scripts/fetch-fonts.mjs`
- Create: `public/fonts/` (tiga berkas woff2, hasil skrip)
- Modify: `src/layouts/Base.astro:35-40` (buang tautan Google Fonts, pasang `@font-face`)
- Modify: `.gitignore` (jangan abaikan `public/fonts/`)

**Interfaces:**
- Consumes: `--font-display`, `--font-body`, `--font-mono` dari Task 1.
- Produces: keluarga `"Bricolage Grotesque"`, `"Literata"`, `"JetBrains Mono"` tersedia tanpa permintaan jaringan keluar.

- [ ] **Step 1: Tulis pengunduh font**

Buat `scripts/fetch-fonts.mjs`:

```js
// Mengunduh subset latin tiga muka huruf ke public/fonts/.
// Dijalankan sekali dengan tangan, bukan bagian dari build — hasilnya
// di-commit supaya build tidak pernah bergantung pada jaringan.
//
// Jalankan: node scripts/fetch-fonts.mjs
import { mkdirSync, writeFileSync } from "node:fs";

const UA =
  "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 " +
  "(KHTML, like Gecko) Chrome/126.0 Safari/537.36";

const FONTS = [
  { q: "Bricolage+Grotesque:opsz,wght@12..96,400..800", file: "bricolage-grotesque.woff2" },
  { q: "Literata:opsz,wght@7..72,400..700", file: "literata.woff2" },
  { q: "JetBrains+Mono:wght@400..600", file: "jetbrains-mono.woff2" },
];

// Blok @font-face yang unicode-range-nya memuat U+0000-00FF adalah subset latin.
function urlLatin(css) {
  for (const b of css.split("@font-face").slice(1)) {
    if (!/U\+0000-00FF/.test(b)) continue;
    const m = b.match(/url\((https:\/\/fonts\.gstatic\.com[^)]+)\)/);
    if (m) return m[1];
  }
  throw new Error("subset latin tidak ditemukan");
}

mkdirSync(new URL("../public/fonts/", import.meta.url), { recursive: true });

for (const f of FONTS) {
  const css = await (
    await fetch(`https://fonts.googleapis.com/css2?family=${f.q}&display=swap`, {
      headers: { "User-Agent": UA },
    })
  ).text();
  const buf = Buffer.from(await (await fetch(urlLatin(css), { headers: { "User-Agent": UA } })).arrayBuffer());
  writeFileSync(new URL(`../public/fonts/${f.file}`, import.meta.url), buf);
  console.log(`${f.file.padEnd(28)} ${(buf.length / 1024).toFixed(1)} KB`);
}
```

- [ ] **Step 2: Jalankan, pastikan tiga berkas ada**

```bash
node scripts/fetch-fonts.mjs && ls -la public/fonts
```

Diharapkan: `bricolage-grotesque.woff2` sekitar 75 KB, `literata.woff2` sekitar
84 KB, `jetbrains-mono.woff2` sekitar 31 KB.

- [ ] **Step 3: Ganti tautan Google Fonts dengan @font-face**

Di `src/layouts/Base.astro`, hapus baris 35–40 (dua `<link rel="preconnect">`
dan satu `<link>` ke `fonts.googleapis.com`). Ganti dengan:

```html
<link
  rel="preload"
  href="/fonts/literata.woff2"
  as="font"
  type="font/woff2"
  crossorigin
/>
<style is:global>
  /* Self-host, jadi tidak ada permintaan pihak ketiga sebelum teks pertama
     tampil. Variable font, satu berkas untuk seluruh rentang berat.
     Diperbarui lewat: node scripts/fetch-fonts.mjs */
  @font-face {
    font-family: "Bricolage Grotesque";
    src: url("/fonts/bricolage-grotesque.woff2") format("woff2");
    font-weight: 400 800;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: "Literata";
    src: url("/fonts/literata.woff2") format("woff2");
    font-weight: 400 700;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: "JetBrains Mono";
    src: url("/fonts/jetbrains-mono.woff2") format("woff2");
    font-weight: 400 600;
    font-style: normal;
    font-display: swap;
  }
</style>
```

Hanya Literata yang di-`preload`: dia muka huruf teks isi, jadi yang paling
menentukan kapan halaman terasa siap.

- [ ] **Step 4: Pastikan public/fonts tidak diabaikan git**

```bash
git check-ignore -v public/fonts/literata.woff2 || echo "tidak diabaikan, aman"
```

Kalau ternyata diabaikan, tambahkan `!public/fonts/` ke `.gitignore`.

- [ ] **Step 5: Setel judul memakai Bricolage 700**

Di `src/styles/global.css`, pada aturan yang menyetel `font-family: var(--font-display)`,
tambahkan:

```css
  font-optical-sizing: auto;
  font-weight: 700;
  letter-spacing: -0.015em;
```

Berat 400 terlalu scrappy untuk tesis ketelitian, 800 kehilangan karakternya.

- [ ] **Step 6: Pakai --t-theme untuk transisi tema**

`--t-theme` didefinisikan di Task 1 tapi belum ada yang memakainya. Tukar tema
berjalan lewat View Transitions API (`Base.astro:171-175`), dan durasinya diatur
CSS, bukan JavaScript. Tambahkan ke `src/styles/global.css`:

```css
/* Durasi tukar tema. Diatur di sini, bukan di skrip: startViewTransition
   tidak menerima durasi, dia membaca animasi pseudo-element ini. */
::view-transition-old(root),
::view-transition-new(root) {
  animation-duration: var(--t-theme);
  animation-timing-function: var(--ease-out);
}
```

- [ ] **Step 7: Bangun dan periksa font benar-benar terpakai**

```bash
npm run build && grep -c "fonts.googleapis.com\|fonts.gstatic.com" dist/index.html
```

Diharapkan: `0`. Kalau bukan nol, masih ada tautan CDN yang tertinggal.

Lalu tekan tombol tema di peramban dan pastikan pergantiannya terasa sekitar
sepertiga detik, bukan seketika dan bukan setengah detik.

- [ ] **Step 8: Commit**

```bash
git add scripts/fetch-fonts.mjs public/fonts src/layouts/Base.astro src/styles/global.css .gitignore
git commit -m "Self-host the type, and set the body in a serif

Body text moves from Public Sans to Literata. This is the change that
decides whether the page reads as an interface or as something
published, and it matters more than the display face. Headings move
from Fraunces to Bricolage Grotesque at 700, whose deliberate
irregularity suits a page whose whole argument is against uniformity.

Serving the files ourselves removes two third-party requests before the
first text paints."
```

---

## Task 3: Empat ruangan, lantai rata

Belum ada gerak. Tugas ini membuktikan strukturnya benar sebelum gerak
menyembunyikan kesalahan.

**Files:**
- Modify: `src/pages/index.astro:20-34`
- Modify: `src/styles/global.css` (tambah `.room`, ganti `.section--ink` baris 222-243)
- Modify: `src/components/Projects.astro` (lepas `.section--ink`)
- Modify: `src/styles/global.css:389-447` (blok `@media print`)

**Interfaces:**
- Consumes: `--rung-1..4`, `--motif-1..4`, `--pen`, `--pen-soft` dari Task 1.
- Produces: `.room[data-rung="N"]` yang mendefinisikan ulang **hanya `--paper` dan `--motif`** untuk seluruh isinya. `--ink` dan `--ink-soft` tidak perlu diulang per ruangan: warna teks sama di keempat lantai dalam satu tema, jadi cukup diwarisi dari `:root`. Task 4 memasang gerak pada selector yang sama.

- [ ] **Step 1: Bungkus sepuluh bagian jadi empat ruangan**

Ganti isi `<Base>` di `src/pages/index.astro` (baris 24–33) dengan:

```astro
  <div class="room" data-rung="1">
    <Hero />
    <About />
    <Timeline />
  </div>

  <div class="room" data-rung="2">
    <Projects />
  </div>

  <div class="room" data-rung="3">
    <Experience />
    <Organisation />
    <Publication />
    <Achievements />
  </div>

  <div class="room" data-rung="4">
    <Skills />
    <Closing />
  </div>
```

Penomoran bagian tidak terpengaruh: `counter-reset: section` ada di `main`
(`global.css:191`), bukan di pembungkus tiap bagian.

- [ ] **Step 2: Tambahkan mekanika ruangan ke global.css**

Ganti `src/styles/global.css` baris 222–243 (blok `.section--ink` beserta
turunan `[data-theme="dark"]`-nya) dengan:

```css
/* ── Ruangan ──────────────────────────────────────────────────────────
   Empat ruangan, lantainya turun satu arah. Tiap ruangan mendefinisikan
   ulang token warnanya, jadi seluruh isinya ikut tanpa satu pun aturan
   tambahan. Ini pola yang dulu dipakai .section--ink untuk satu blok,
   diperluas jadi empat.

   Lantai adalah NOMOR LANGKAH, bukan warna. Tema memutuskan tangga mana
   yang dipakai, jadi satu himpunan aturan melayani dua tema. */
.room {
  position: relative;
  background: var(--paper);
  color: var(--ink);
  isolation: isolate;
}

.room[data-rung="1"] { --paper: var(--rung-1); --motif: var(--motif-1); }
.room[data-rung="2"] { --paper: var(--rung-2); --motif: var(--motif-2); }
.room[data-rung="3"] { --paper: var(--rung-3); --motif: var(--motif-3); }
.room[data-rung="4"] { --paper: var(--rung-4); --motif: var(--motif-4); }

/* Pergantian lantai sudah jadi pemisah, jadi garis atas bagian pertama
   tiap ruangan dibuang supaya tidak ada dua pemisah bertumpuk. */
.room > .section:first-child {
  border-top: none;
}
```

- [ ] **Step 3: Lepas .section--ink dari Projects**

Di `src/components/Projects.astro`, cari `class="section section--ink"` dan
ganti jadi `class="section section--bleed"`. Lalu tambahkan ke `global.css`:

```css
/* Satu-satunya bagian yang melanggar lebar baca. Pelanggaran ukuran itu
   sendiri jadi penanda, menggantikan kontras lantai yang dulu menyorot
   bagian ini. */
.section--bleed .shell {
  max-width: var(--page-wide);
}
```

Kalau `Projects.astro` masih memuat aturan yang mengandalkan latar gelapnya,
hapus aturan itu — warnanya sekarang datang dari ruangan.

- [ ] **Step 4: Perbaiki blok print yang menyebut .section--ink**

Di `src/styles/global.css` blok `@media print`, ganti `.section--ink` jadi
`.room`, dan tambahkan pengembalian lantai ke putih:

```css
  .room {
    --paper: #fff;
    --motif: #fff;
    background: #fff !important;
    color: #000 !important;
  }
```

Sekalian hapus `.wipe` dari daftar di blok print — class itu sudah dibuang saat
audit 14 Agustus dan rujukannya tertinggal.

- [ ] **Step 5: Bangun, lalu periksa empat lantai di peramban**

```bash
npm run build && npm run preview
```

Buka pratinjaunya, lalu jalankan di konsol:

```js
[...document.querySelectorAll(".room")].map((r) => ({
  rung: r.dataset.rung,
  paper: getComputedStyle(r).backgroundColor,
}));
```

Diharapkan: empat nilai berbeda, makin gelap dari ruangan satu ke empat.
Ulangi setelah menekan tombol tema; harus tetap empat nilai berbeda, dan
ruangan satu harus **lebih terang** dari ruangan empat di kedua tema.

- [ ] **Step 6: Periksa penomoran bagian tidak mengulang per ruangan**

Di konsol peramban:

```js
[...document.querySelectorAll(".section__label")].map((l) => l.textContent.trim().slice(0, 2));
```

Diharapkan: **sembilan** label, berjalan `01` sampai `09` tanpa mengulang dari
`01` di tiap ruangan.

Sembilan, bukan sepuluh: **Hero tidak punya label bernomor** dan tidak pernah
punya. Itu keadaan sebelum redesign, bukan sesuatu yang tugas ini ubah. Yang
diuji langkah ini adalah bahwa membungkus bagian ke dalam ruangan tidak
mengganggu `counter-reset: section` yang tinggal di `main`. Kalau angkanya
mengulang dari 01 di ruangan kedua, berarti ada `counter-reset` yang tanpa
sengaja ikut masuk ke pembungkus ruangan.

- [ ] **Step 7: Commit**

```bash
git add src/pages/index.astro src/styles/global.css src/components/Projects.astro
git commit -m "Group the ten sections into four rooms

The floor steps down once per act and never returns. Each room redefines
its own colour tokens, so everything inside follows without a single
extra rule — the pattern the old ink block already used for one section,
widened to four.

A floor is a step number, not a colour. The theme decides which ladder
those steps come from, so one set of rules serves both themes and dark
mode cannot break the order of the journey.

Projects loses its dark ground and keeps its emphasis by breaking the
measure instead."
```

---

## Task 4: Gerak pergantian ruangan

**Files:**
- Modify: `src/styles/global.css` (blok `@supports (animation-timeline: view())` sekitar baris 330, dan blok `prefers-reduced-motion` sekitar baris 449)

**Interfaces:**
- Consumes: `.room[data-rung]` dari Task 3.
- Produces: `.room::before` sebagai selimut pengelupas dan `.room > *` sebagai isi yang tertinggal. Keduanya pseudo-element atau selector anak, jadi **tidak ada perubahan markup** — Task 8 nanti memakai `.room::after`, jadi jangan pakai `::after` di sini.

- [ ] **Step 1: Tambahkan selimut pengelupas**

Tambahkan ke `src/styles/global.css`, setelah blok `.room`:

```css
/* Selimut berisi warna lantai SEBELUMNYA. Saat ruangan masuk layar,
   selimut mengelupas ke atas dan lantai baru muncul dari bawah, dengan
   satu garis teal menandai tepinya. Ruangan satu tidak punya selimut
   karena tidak ada lantai sebelumnya. */
.room::before {
  content: "";
  position: absolute;
  inset: 0;
  z-index: -1;
  transform-origin: top;
  border-bottom: 1px solid var(--verified);
  pointer-events: none;
}

.room[data-rung="1"]::before { display: none; }
.room[data-rung="2"]::before { background: var(--rung-1); }
.room[data-rung="3"]::before { background: var(--rung-2); }
.room[data-rung="4"]::before { background: var(--rung-3); }
```

- [ ] **Step 2: Gerakkan selimut dan isinya dengan gulir**

Tambahkan di **dalam** blok `@supports (animation-timeline: view())` yang sudah
ada di `global.css` (sekitar baris 330):

```css
  /* Rentang sengaja sempit: pergantian selesai lebih awal, pembaca tidak
     menunggu. Rentang gulir tidak punya durasi — mempercepatnya berarti
     mempersempit rentangnya. */
  .room::before {
    animation: peel linear both;
    animation-timeline: view();
    animation-range: entry 20% entry 70%;
  }

  /* Isi bergerak lebih lambat daripada lantainya, jadi ada rasa kedalaman.
     Dipasang di anak langsung supaya pseudo-element selimut tidak ikut. */
  .room > * {
    animation: drift linear both;
    animation-timeline: view();
    animation-range: entry 0% exit 100%;
  }
```

Dan di luar blok `@supports`, bersama `@keyframes` lain sekitar baris 310:

```css
@keyframes peel {
  from { transform: scaleY(1); }
  to { transform: scaleY(0); }
}

@keyframes drift {
  from { transform: translateY(2rem); }
  to { transform: translateY(-2rem); }
}
```

- [ ] **Step 3: Matikan gerak saat reduced-motion, tapi biarkan lantai berganti**

Di blok `@media (prefers-reduced-motion: reduce)` (sekitar baris 449),
tambahkan ke daftar selector yang sudah ada:

```css
  .room::before,
  .room > * {
    animation-timeline: auto !important;
    animation: none !important;
    transform: none !important;
  }

  /* Selimut dibuang, bukan dibekukan — kalau dibekukan pada scaleY(1) dia
     menutupi lantai baru dan seluruh halaman terlihat satu warna. */
  .room::before {
    display: none !important;
  }
```

Ini bagian yang paling mudah salah. Linimasa gulir mengabaikan
`animation-duration`, jadi reset biasa tidak berpengaruh, dan selimut yang
dibekukan pada keadaan awal justru menyembunyikan lantainya.

- [ ] **Step 4: Verifikasi di peramban, tiga keadaan**

```bash
npm run build && npm run preview
```

1. Gulir pelan. Tiap batas ruangan harus memperlihatkan warna lama mengelupas
   ke atas dengan garis teal di tepinya.
2. Nyalakan reduced-motion di sistem, muat ulang. Gerak hilang, tapi keempat
   lantai **tetap** berbeda warna. Jalankan lagi pemeriksaan `backgroundColor`
   dari Task 3 Step 5 untuk memastikan.
3. Di konsol, pastikan selimut benar-benar dianimasikan:

```js
getComputedStyle(document.querySelector('.room[data-rung="2"]'), "::before").animationName;
```

Diharapkan: `"peel"`. Kalau `"none"`, peramban tidak mendukung
`animation-timeline` dan fallback `@supports` sedang bekerja — itu benar, bukan
kesalahan.

- [ ] **Step 5: Commit**

```bash
git add src/styles/global.css
git commit -m "Move the floor changes, not just their colours

A room now enters by peeling its predecessor's floor upward, with one
teal line marking the edge, while the content inside lags two rem
behind. The colour delta alone was not enough to feel like a change of
place.

Scroll timelines ignore animation-duration, so reduced motion has to
cancel them by name. The peel is removed rather than frozen: frozen at
its start it covers the new floor and the whole page reads as one
colour."
```

---

## Task 5: Setel ulang rentang gerak yang sudah ada

**Dua gerak dari PRD sudah terpasang di repo ini.** Linimasa yang terisi ada di
`Timeline.astro:98-104`, dan angka yang terbit berurutan ada sebagai `.rise-group`
di `global.css:339-356`. Yang salah bukan mekanismenya, cuma rentangnya. Jangan
bangun ulang; setel.

Titik Wisuda juga sudah kosong lewat `.track__point--open` (`Timeline.astro:134`),
dan itu benar — titik kosong menyampaikan sesuatu yang benar dan tidak boleh
diisi oleh gerak.

**Files:**
- Modify: `src/components/Timeline.astro:102` (satu baris)
- Modify: `src/styles/global.css:339-356`
- Modify: dua komponen yang memuat chip angka

**Interfaces:**
- Consumes: `.rise-group` dan `grow-y` yang sudah ada. Tidak menghasilkan nama baru.

- [ ] **Step 1: Setel rentang linimasa ke nilai PRD**

Di `src/components/Timeline.astro` baris 102, ganti:

```css
      animation-range: entry 65% exit 35%;
```

jadi:

```css
      animation-range: entry 55% cover 75%;
```

Yang lama terus mengisi sampai jauh setelah bagiannya keluar layar, jadi garisnya
tertinggal di belakang pembaca. Yang baru selesai saat bagiannya masih terlihat.

- [ ] **Step 2: Buat kelas terpisah untuk jeda chip angka**

**Jangan menyetel `.rise-group`.** Kelas itu ternyata dipakai di delapan tempat
dan bukan cuma untuk chip angka: `About` memakainya untuk paragraf, `Organisation`
untuk galeri foto, `Projects` untuk tangkapan layar. Menyetel rentangnya secara
global membuat paragraf baru muncul setelah 40% masuk layar — perubahan alur baca
yang tidak diminta PRD. Menguranginya jadi dua tempat malah menghapus reveal dari
enam tempat yang tidak berhubungan.

Yang dibatasi PRD adalah **jeda chip angka**, bukan seluruh reveal berurutan.
Jadi buat kelas sendiri. Tambahkan di dalam blok
`@supports (animation-timeline: view())` yang sudah ada:

```css
  /* Jeda khusus chip angka, lebih lambat dan lebih berjenjang daripada
     .rise-group. Kelas sendiri, bukan menyetel .rise-group, karena kelas itu
     juga menaungi paragraf dan galeri yang tidak boleh ikut melambat. */
  .rise-figures > * {
    animation: rise linear both;
    animation-timeline: view();
    animation-range: entry 40% entry 85%;
  }
  .rise-figures > *:nth-child(2) { animation-range: entry 45% entry 90%; }
  .rise-figures > *:nth-child(3) { animation-range: entry 50% entry 95%; }
  .rise-figures > *:nth-child(4) { animation-range: entry 55% cover 5%; }
  .rise-figures > *:nth-child(n + 5) { animation-range: entry 60% cover 10%; }
```

Tambahkan juga `.rise-figures > *` ke daftar selector di blok
`@media (prefers-reduced-motion: reduce)`, bersama `.rise` dan `.rise-group > *`
yang sudah ada di sana.

Jangan sentuh `.rise` tunggal maupun `.rise-group`. PRD tidak menyebut nilai
untuk keduanya, dan yang sekarang sudah bekerja.

- [ ] **Step 3: Pasang di tepat dua tempat**

Dua pembungkus yang benar-benar berisi chip angka, dan hanya dua ini:

- `src/components/Hero.astro:66` — `<dl class="hero__figures rise-group">`
- `src/components/Achievements.astro:19` — `<dl class="bento rise-group">`

Pada kedua berkas itu, **tambahkan** `rise-figures` dan **hapus** `rise-group`
dari class-nya, supaya tidak ada dua aturan jeda yang berebut elemen yang sama.
Jangan sentuh enam pemakaian `rise-group` lainnya.

- [ ] **Step 4: Verifikasi**

```bash
npm run build && npm run preview
```

- Gulir melewati linimasa. Garis teal wajib selesai mengisi **sebelum** bagian
  linimasa keluar dari layar, bukan sesudahnya.
- Titik Nov 2026 wajib tetap kosong.
- Gulir melewati kedua tempat chip, pastikan masuk bergantian bukan serentak.
- Nyalakan reduced-motion, pastikan semua chip dan garis langsung terlihat penuh.

- [ ] **Step 5: Commit**

```bash
git add src/components/Timeline.astro src/styles/global.css src/components
git commit -m "Retune the scroll ranges the page already had

The timeline fill and the staggered figures were both already built.
Their ranges were the problem: the fill kept growing well after its
section had left the screen, so the teal line trailed the reader instead
of following them.

The stagger is limited to two places. Everywhere at once turns a reveal
into a tic."
```

---

## Task 6: Penahanan sticky pada studi kasus audit

**Tugas ini jauh lebih kecil dari versi pertamanya, dan alasannya penting.**
Versi pertama menyuruh membangun `.pipe__step[data-open]` dengan skrip sendiri
untuk membuka tahap audit per klik. Itu ditulis dari halaman contoh, bukan dari
repo ini. Di repo ini yang diminta **sudah ada, dan dalam bentuk yang lebih
baik**:

- `Projects.astro:144-157` sudah punya `<ol class="pipeline">` berisi empat
  `<li class="pipeline__step">`, masing-masing `<details name="ktm-pipeline">`,
  bernomor `01`–`04` lewat `.pipeline__no`. Itu akordeon eksklusif berbasis
  elemen asli: bekerja tanpa JavaScript, punya dukungan papan tik dan pembaca
  layar bawaan, dan tidak butuh satu baris skrip.
- `AuditDiagram.astro` sudah interaktif lewat tombol `Rebuild the split` dengan
  `aria-pressed`, memindahkan chip dengan View Transitions, dan menampilkan
  `48 of 165` menjadi `0 of 165`.

Mengganti keduanya dengan pola `data-open` buatan sendiri akan menukar elemen
asli yang aksesibel dengan ARIA tulis tangan. Itu regresi, bukan kemajuan.

Yang benar-benar belum ada cuma satu: **penahanan**. PRD menyorot Proyek lewat
waktu, dan waktu berarti satu ruangan yang menahan pembaca lebih lama.

**Files:**
- Modify: `src/components/Projects.astro` (pembungkus sticky di sekitar studi kasus audit)

**Interfaces:**
- Consumes: `--verified`, `--before-audit`, `--rule` dari Task 1; `.room[data-rung="2"]` dari Task 3.
- Produces: `.audit--hold`. **Tidak** menghasilkan `.pipe__step` maupun skrip baru.

- [ ] **Step 1: Pastikan dulu bahwa memang tidak perlu dibangun**

```bash
grep -n "pipeline__step\|details name\|pipeline__no" src/components/Projects.astro
grep -n "aria-pressed\|view-transition-name" src/components/AuditDiagram.astro
```

Kalau keduanya mengembalikan hasil seperti yang dijelaskan di atas, jangan
bangun apa pun untuk pembukaan per tahap. Kalau ternyata tidak ada, berhenti
dan laporkan BLOCKED — berarti asumsi tugas ini keliru.

- [ ] **Step 2: Buat studi kasus audit menahan pembaca**

Bungkus bagian dalam `.audit` supaya papan diagramnya menempel sementara prosa
di sekitarnya terus bergulir. Tambahkan ke blok `<style>` di `Projects.astro`:

```css
  /* Satu-satunya penahanan baru di halaman. Yang pertama pelat spesifikasi di
     Experience, jadi ini yang kedua dan terakhir — PRD melarang lebih dari dua.
     Papan diagram menempel sementara prosa di sekitarnya bergulir, sehingga
     pembaca menghabiskan lebih banyak detik di bagian ini daripada di mana pun.
     Itulah cara Proyek disorot, menggantikan kontras lantai. */
  .audit--hold {
    position: sticky;
    top: 4rem;
  }

  /* Penahanan hanya berlaku kalau ada ruang untuk menahan. Di layar pendek
     posisi sticky justru memakan tinggi baca. */
  @media (max-height: 44rem) {
    .audit--hold {
      position: static;
    }
  }
```

Pasang `audit--hold` pada pembungkus `<AuditDiagram />`. Kalau `<AuditDiagram />`
belum punya pembungkus sendiri, buat satu `<div class="audit--hold">` di
sekitarnya. **Jangan** memindahkan `<AuditDiagram />` keluar dari `.audit`, dan
jangan menyentuh urutan `audit__gist` → diagram → `deep--audit`.

Penahanannya butuh ruang untuk terasa: pastikan tinggi total `.audit` cukup
sehingga papan menempel selama sekitar satu layar penuh, bukan lebih. Kalau
lebih lama dari itu pembaca merasa terkunci, bukan dituntun.

- [ ] **Step 3: Periksa tidak ada penahanan ketiga**

```bash
grep -rn "position: sticky" src/
```

Diharapkan **tiga** hasil, dan itu benar: pelat spesifikasi di
`Experience.astro`, `.audit--hold` yang baru, dan masthead di `Base.astro`.

Yang dibatasi PRD adalah **bagian isi yang menahan pembaca**, bukan setiap
pemakaian `position: sticky`. Masthead itu kelengkapan navigasi — dia tidak
menahan siapa pun, cuma tetap terjangkau. Jadi dua penahanan isi ditambah satu
chrome sudah patuh. Kalau muncul penahanan isi keempat, itu yang harus dihapus.

- [ ] **Step 4: Verifikasi**

```bash
npm run build && npm run preview
```

- Gulir melewati studi kasus audit. Papan wajib menempel lalu lepas, dan tidak
  boleh menimpa prosa di bawahnya saat lepas.
- Tekan tombol `Rebuild the split` dengan papan dalam keadaan menempel. Chip
  wajib tetap berpindah dan angka berubah dari `48 of 165` ke `0 of 165`.
- Navigasi dengan Tab. Tombol switch dan keempat `summary` pipeline wajib punya
  cincin fokus yang terlihat.
- Matikan JavaScript. Keempat tahap pipeline wajib tetap bisa dibuka, karena
  `<details>` tidak bergantung pada skrip. Papan diagram akan berhenti di
  keadaan `before`, dan itu benar.
- Nyalakan reduced-motion. Penahanan tetap berlaku — sticky bukan animasi — tapi
  View Transitions pada chip wajib mati.
- Perkecil tinggi jendela di bawah 44rem, pastikan penahanan berhenti.

- [ ] **Step 5: Commit**

```bash
git add src/components/Projects.astro
git commit -m "Hold the reader at the audit case study

Projects is the room that holds. That is how it keeps its emphasis now
that the floor no longer goes dark under it: the reader spends more
seconds here than anywhere else on the page, and seconds cannot be
skipped past the way a colour can.

No per-stage disclosure was built. The page already had it, in a better
form than the plan proposed: the pipeline is four native details
elements, numbered, exclusive, working with JavaScript off. Replacing
that with hand-rolled ARIA would have been a regression."
```

---

## Task 7: 1.645 titik yang menata ulang diri

Satu-satunya objek bergerak di seluruh halaman.

**Files:**
- Create: `src/components/DatasetSplit.astro`
- Modify: `src/components/Projects.astro` (pasang komponennya di studi kasus audit)

**Interfaces:**
- Consumes: `--verified`, `--before-audit`, `--pen-soft`.
- Produces: komponen `DatasetSplit` tanpa props.

- [ ] **Step 1: Buat komponennya**

Buat `src/components/DatasetSplit.astro`:

```astro
---
// Satu-satunya objek bergerak di halaman ini. Bukan hiasan: yang
// digambarkan temuan utama skripsi, yaitu 48 dari 165 citra uji tercemar
// karena citra palsu dibuat dari kartu asli dengan penomoran sama.
//
// Sumber angka: HASIL_UJICOBA_TAMBAHAN/README.md, 25 Juli 2026.
//
// Digambar 165 titik, bukan 1.645. Yang diaudit split ujinya, dan 165 itu
// jumlah citra uji pada split 80:10:10. Menggambar 1.645 titik akan
// menyiratkan seluruh dataset tercemar, dan itu tidak benar.
const TOTAL = 165;
const TERCEMAR = 48;

// Yang tercemar disebar merata, bukan dikelompokkan, karena itulah
// masalahnya: skrip split mengacak real dan fake secara terpisah, jadi
// kartu yang tercemar tersebar ke seluruh set uji.
//
// Sebaran gaya Bresenham: menghasilkan TEPAT 48 titik dengan jarak antar
// titik 3 sampai 4, jadi merata sungguhan. Trik modulo seperti
// `(i * 7) % 165 < 48` juga menghasilkan 48, tapi menggumpal jadi
// rentetan tujuh lalu jeda delapan belas — persis kebalikan dari yang
// digambarkan.
const bocor = (i) =>
  Math.floor(((i + 1) * TERCEMAR) / TOTAL) > Math.floor((i * TERCEMAR) / TOTAL);

const titik = Array.from({ length: TOTAL }, (_, i) => ({ bocor: bocor(i), i }));
---

<figure class="split">
  <div class="split__grid" aria-hidden="true">
    {titik.map((t) => <i class:list={["split__dot", { "is-leak": t.bocor }]} />)}
  </div>
  <figcaption>
    165 citra uji pada split 80:10:10. Yang bertanda terakota tercemar oleh
    kebocoran tingkat kartu: 48 citra, 29,1%. Gulir untuk melihat split
    dibangun ulang group-aware.
  </figcaption>
</figure>

<style>
  .split { margin: 2.5rem 0 0; }

  .split__grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, 0.55rem);
    gap: 0.3rem;
    justify-content: center;
  }

  .split__dot {
    width: 0.55rem;
    height: 0.55rem;
    border-radius: 50%;
    background: var(--pen-soft);
    display: block;
  }

  .split__dot.is-leak { background: var(--before-audit); }

  figcaption {
    font-size: var(--size-caption);
    color: var(--ink-soft);
    max-width: var(--measure);
    margin: 1.25rem auto 0;
    text-align: center;
  }

  /* Titik yang bocor bergeser ke kelompoknya sendiri saat bagian ini
     dilewati: itulah yang dikerjakan resplit_group_aware.py. Yang
     dianimasikan transform dan warna, bukan properti tata letak, jadi
     tidak ada reflow. */
  @supports (animation-timeline: view()) {
    @media (prefers-reduced-motion: no-preference) {
      .split__dot.is-leak {
        animation: regroup linear both;
        animation-timeline: view();
        animation-range: entry 35% cover 55%;
      }

      @keyframes regroup {
        from {
          background: var(--before-audit);
          transform: none;
        }
        to {
          background: var(--verified);
          transform: translateY(-0.85rem);
        }
      }
    }
  }
</style>
```

- [ ] **Step 2: Pasang di studi kasus audit**

Di `src/components/Projects.astro`, tambahkan importnya di frontmatter:

```astro
import DatasetSplit from "./DatasetSplit.astro";
```

lalu `<DatasetSplit />` di dalam studi kasus audit, setelah paragraf yang
menyebut kebocoran 29%. Jangan mengubah paragrafnya.

- [ ] **Step 3: Verifikasi jumlah dan gerak**

```bash
npm run build && npm run preview
```

Di konsol:

```js
({
  total: document.querySelectorAll(".split__dot").length,
  bocor: document.querySelectorAll(".split__dot.is-leak").length,
});
```

Diharapkan tepat: `{ total: 165, bocor: 48 }`. Angka ini harus benar — kalau
salah, halaman menyatakan sesuatu yang tidak didukung dokumen.

Gulir melewatinya, pastikan titik terakota bergeser naik dan berubah teal.
Nyalakan reduced-motion, pastikan titiknya diam dan tetap terakota, dan
keterangannya masih menjelaskan apa yang seharusnya terjadi.

- [ ] **Step 4: Commit**

```bash
git add src/components/DatasetSplit.astro src/components/Projects.astro
git commit -m "Draw the leak, and the regrouping that fixed it

The only moving object on the page, and it carries evidence rather than
decoration: forty-eight of one hundred and sixty-five test images,
spread evenly because that was the fault — the split script shuffled
real and fake independently, so a card family landed on both sides.

One hundred and sixty-five dots, not one thousand six hundred and
forty-five. What was audited is the test split. Drawing the whole
dataset would imply all of it was contaminated, and it was not."
```

---

## Task 8: Tekstur ubin

Lapisan terakhir, sengaja. Kalau ada masalah muncul setelah ini, penyebabnya
tidak ambigu.

**Files:**
- Create: `scripts/prep-texture.mjs`
- Create: `src/assets/motif-tile.webp` (hasil skrip)
- Modify: `src/styles/global.css` (tambah lapisan tekstur pada `.room`)

**Interfaces:**
- Consumes: `assets/background.jpeg` (736×1472, grayscale 0–55, motif berulang sekitar 368 px), `--motif` dari Task 1.
- Produces: `src/assets/motif-tile.webp`, ubin 736×736 dengan pola di kanal alfa.

- [ ] **Step 1: Tulis pengolah tekstur**

Buat `scripts/prep-texture.mjs`:

```js
// assets/background.jpeg → src/assets/motif-tile.webp
//
// Berkas sumbernya wallpaper ponsel 736x1472: tepinya tidak menyambung dan
// ada vignette halus di kiri-kanan, jadi tidak bisa langsung diubin.
//
// Tiga hal yang dikerjakan:
//   1. Ambil satu satuan ulang 368x368 dari tengah, menjauhi vignette.
//   2. Cerminkan jadi 736x736. Ubin bercermin selalu tanpa jahitan menurut
//      konstruksinya. Simetri cerminnya tidak terlihat karena pada ΔL* 1,5
//      motifnya nyaris tak terlihat.
//   3. Normalisasi. Sumbernya cuma memakai 0-55 dari 255, jadi tanpa
//      peregangan polanya hampir hilang begitu dipakai sebagai alfa.
//
// Polanya ditulis ke kanal ALFA, bukan luminance: mask-image memakai alfa
// secara baku di semua peramban, sedangkan mask-mode: luminance dukungannya
// belum merata.
//
// Jalankan: node scripts/prep-texture.mjs
import sharp from "sharp";

const SUMBER = new URL("../assets/background.jpeg", import.meta.url).pathname.slice(1);
const KELUARAN = new URL("../src/assets/motif-tile.webp", import.meta.url).pathname.slice(1);

const UNIT = 368;
const KIRI = Math.round((736 - UNIT) / 2);
const ATAS = 300;

const unit = await sharp(SUMBER)
  .extract({ left: KIRI, top: ATAS, width: UNIT, height: UNIT })
  .greyscale()
  .normalise()
  .toBuffer();

const cermin = async (flip, flop) =>
  sharp(unit).flip(flip).flop(flop).toBuffer();

const pola = await sharp({
  create: { width: UNIT * 2, height: UNIT * 2, channels: 1, background: { r: 0 } },
})
  .composite([
    { input: unit, left: 0, top: 0 },
    { input: await cermin(false, true), left: UNIT, top: 0 },
    { input: await cermin(true, false), left: 0, top: UNIT },
    { input: await cermin(true, true), left: UNIT, top: UNIT },
  ])
  .png()
  .toBuffer();

// Putih penuh, dengan polanya sebagai alfa. Warnanya nanti datang dari
// --motif lewat CSS, jadi satu berkas melayani delapan lantai.
await sharp({
  create: { width: UNIT * 2, height: UNIT * 2, channels: 3, background: "#ffffff" },
})
  .joinChannel(await sharp(pola).greyscale().raw().toBuffer(), {
    raw: { width: UNIT * 2, height: UNIT * 2, channels: 1 },
  })
  .webp({ quality: 82, alphaQuality: 90 })
  .toFile(KELUARAN);

const m = await sharp(KELUARAN).metadata();
console.log(`motif-tile.webp  ${m.width}x${m.height}  ${(m.size / 1024).toFixed(1)} KB  alfa: ${m.hasAlpha}`);
```

- [ ] **Step 2: Jalankan dan periksa hasilnya**

```bash
node scripts/prep-texture.mjs
```

Diharapkan: `736x736`, punya alfa, di bawah 80 KB. Kalau `hasAlpha: false`,
`joinChannel` gagal dan mask tidak akan berfungsi.

- [ ] **Step 3: Pasang lapisan tekstur**

Tambahkan ke `src/styles/global.css`, setelah aturan `.room`:

```css
/* Tekstur ornamen, ΔL* 1,5 dari lantainya. Satu berkas melayani delapan
   lantai: berkasnya cuma peta transparansi, warnanya dari --motif.

   Tanpa background-attachment: fixed — itu memaksa repaint tiap frame
   gulir, dan halaman ini seluruhnya digerakkan gulir. */
.room::after {
  content: "";
  position: absolute;
  inset: 0;
  z-index: -1;
  background: var(--motif);
  mask-image: url("../assets/motif-tile.webp");
  -webkit-mask-image: url("../assets/motif-tile.webp");
  mask-repeat: repeat;
  -webkit-mask-repeat: repeat;
  mask-size: 368px 368px;
  -webkit-mask-size: 368px 368px;
  pointer-events: none;
}

@media print {
  .room::after { display: none; }
}
```

`z-index: -1` menaruhnya di bawah isi tapi di atas latar ruangan. Selimut
pengelupas dari Task 4 juga di `-1`; selimut datang lebih dulu dalam urutan
sumber, jadi tekstur menimpanya. Itu benar — lantai yang mengelupas ikut
bertekstur.

- [ ] **Step 4: Jalankan pemeriksa kontras lagi**

```bash
npm run check:contrast
```

Wajib tetap `0 gagal`. Skrip ini sudah memperhitungkan keadaan bertekstur sejak
Task 1, jadi kalau sekarang gagal berarti nilai `--motif-*` diubah tanpa
dihitung ulang.

- [ ] **Step 5: Periksa jahitan dengan mata**

```bash
npm run build && npm run preview
```

Perbesar peramban ke 200% dan telusuri batas ubin. Cari garis lurus vertikal
atau horizontal yang berulang setiap 368 px. Kalau ada, `ATAS` di skrip
mengenai satuan ulang yang salah — geser nilainya dan jalankan ulang.

Lalu tekan tombol tema dan pastikan motifnya ikut berbalik: di tema gelap
motifnya harus **lebih terang** dari lantainya.

- [ ] **Step 6: Commit**

```bash
git add scripts/prep-texture.mjs src/assets/motif-tile.webp src/styles/global.css
git commit -m "Add the ornament texture, one file for eight floors

The tile carries the pattern in its alpha channel and takes its colour
from the room, so a single file serves four floors in each theme and
inverts correctly in dark mode.

The source is a phone wallpaper: its edges do not meet and it uses only
the bottom fifth of the tonal range. So one repeat unit is taken from
the middle, away from the vignette, mirrored into a tile that is
seamless by construction, and normalised before it becomes alpha.

Held to a lightness delta of 1.5, which the contrast checker enforces:
the darkest point of the pattern is what sets real contrast, not the
floor colour behind it."
```

---

## Task 9: Audit akhir

**Files:** tidak ada yang diubah kecuali ada temuan.

- [ ] **Step 1: Bundel JavaScript wajib tetap nol**

```bash
npm run build && ls dist/_astro/*.js 2>/dev/null | wc -l
```

Diharapkan: `0`. Kalau bukan nol, ada komponen yang tanpa sengaja jadi island.

- [ ] **Step 2: Tidak ada permintaan pihak ketiga**

```bash
grep -oE 'https?://[^"'"'"' )]+' dist/index.html | grep -v dwiyuda | sort -u
```

Yang boleh muncul cuma tautan keluar yang disengaja: GitHub, LinkedIn, DOI.
Tidak boleh ada `fonts.googleapis.com` atau `fonts.gstatic.com`.

- [ ] **Step 3: Kata yang menempel**

```bash
grep -oE '[a-z]<(a|em|code|span)|</span>[a-z]' dist/index.html
```

Diharapkan: tidak ada keluaran. Jebakan ini sudah menggigit empat kali
sekaligus. Periksa hasil build, bukan berkas sumber.

- [ ] **Step 4: Kontras, kedua tema**

```bash
npm run check:contrast
```

Diharapkan: `26 kombinasi diperiksa, 0 gagal.`

- [ ] **Step 5: Empat lebar layar**

Uji pada 375, 768, 1024, dan 1440 px. Pada masing-masing:

```js
({
  gulirSamping: document.documentElement.scrollWidth > document.documentElement.clientWidth,
  lebar: innerWidth,
});
```

`gulirSamping` wajib `false` di keempatnya. Periksa juga tabel dan diagram
punya `overflow-x: auto` sendiri, bukan memaksa badan halaman melebar.

- [ ] **Step 6: Reduced-motion**

Nyalakan di sistem, muat ulang, lalu pastikan tiga hal sekaligus:

1. Tidak ada yang bergerak.
2. Keempat lantai **tetap** berbeda warna.
3. Tidak ada isi yang hilang — khususnya titik dataset dan tahap audit.

- [ ] **Step 7: Papan tik saja**

Tab dari atas ke bawah tanpa menyentuh tetikus. Cincin fokus wajib terlihat
di setiap kendali, termasuk tahap audit dan tombol tema. Lightbox wajib bisa
ditutup dengan Escape.

- [ ] **Step 8: Cetak**

Cetak ke PDF. Lantai wajib jadi putih, tekstur hilang, seluruh tahap audit
tercetak walau tertutup di layar, dan tidak ada gerak yang membekukan isi
pada keadaan tak terlihat.

- [ ] **Step 9: Catat hasilnya ke vault**

Ikuti Prompt 4 di `Portofolio Dwi Yuda - Transfer Context.md`: entri baru di
paling atas `Portofolio Dwi Yuda - Log Sesi.md`, perbarui `diperbarui` pada
tiap catatan yang disentuh, perbarui bagian Kendala Terbuka di beranda proyek,
dan tambahkan jebakan teknis baru ke Catatan Penting kalau ada.

- [ ] **Step 10: Jangan gabungkan ke main**

Berhenti di sini. Dorong branch `redesign`, ambil URL preview dari Cloudflare
Pages, dan minta persetujuan. `main` masih dipakai untuk pendaftaran Apple
Developer Academy.

```bash
git push -u origin redesign
```

---

## Yang Sengaja Tidak Ada di Rencana Ini

- **Kerangka tes.** Repo ini tidak punya dan tidak butuh satu untuk CSS.
  Satu-satunya logika yang benar-benar bisa salah tanpa terlihat adalah
  matematika kontras, dan itu punya pemeriksa sendiri di Task 1.
- **Menyentuh teks.** Tidak satu tugas pun mengubah kalimat.
- **Menggabungkan ke `main`.** Di luar lingkup; keputusan manusia.
- **Nohemi.** Lisensinya belum dipastikan.
- **Custom domain.** Langkah dashboard Cloudflare, bukan kode.
